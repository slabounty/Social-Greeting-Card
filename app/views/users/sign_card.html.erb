<h1>Sign Card for <%= @card.recipient.first_name %> <%= @card.recipient.last_name %></h1>

<%= form_tag ("do_sign") do %>
    <%= label_tag("Add A Message:") %>
    <%= text_field_tag(:message) %>
    <%= hidden_field_tag :card, @card.id %>
    <%= submit_tag("Submit") %>

<% end %>
